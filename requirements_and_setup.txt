# requirements.txt
Flask==2.3.3
gymnasium==0.29.1
pandas==2.0.3
numpy==1.25.2
matplotlib==3.7.2
stable-baselines3==2.1.0
joblib==1.3.2
yfinance==0.2.28
scikit-learn==1.3.0
Werkzeug==2.3.7
Jinja2==3.1.2

# 프로젝트 디렉토리 구조:
# stock_analyzer/
# ├── app.py                    (메인 Flask 애플리케이션)
# ├── requirements.txt          (의존성 패키지 목록)
# ├── templates/
# │   └── index.html           (HTML 템플릿)
# ├── static/                  (CSS/JS 파일, 선택사항)
# ├── models/                  (학습된 모델 파일들)
# │   ├── improved_ppo_model.zip
# │   └── improved_scaler.pkl
# └── README.md

# 설치 및 실행 방법:

# 1. 가상환경 생성 (권장)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 또는
venv\Scripts\activate     # Windows

# 2. 패키지 설치
pip install -r requirements.txt

# 3. 모델 파일 준비
# improved_ppo_model.zip과 improved_scaler.pkl 파일을 
# 프로젝트 루트 디렉토리에 복사

# 4. Flask 애플리케이션 실행
python app.py

# 5. 브라우저에서 http://localhost:5000 접속

# 추가 설정 (production 환경):
# - 환경변수로 SECRET_KEY 설정
# - WSGI 서버 사용 (gunicorn 등)
# - 리버스 프록시 설정 (nginx 등)
# - HTTPS 적용